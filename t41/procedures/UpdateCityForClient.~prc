create or replace procedure UpdateCityForClient(clientId1 in number, cityName1 in varchar2) is
begin
  
UPDATE 
        client
    SET  
        cityname = cityName1
    WHERE 
        clientid = clientId1;
        
end UpdateCityForClient;
/
